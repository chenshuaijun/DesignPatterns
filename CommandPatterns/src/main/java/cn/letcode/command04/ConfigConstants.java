package cn.letcode.command04;

import java.io.File;

public class ConfigConstants {
    public static String path = System.getProperty("user.dir") + File.separator + "CommandPatterns" + File.separator + "src" + File.separator + "icon";
}
